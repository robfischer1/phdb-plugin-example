[phdb]
manifest_version = 1

[plugin]
name = "weather_log"
version = "0.1.0"
description = "Daily weather log JSON ingester (example third-party plugin)"
kind = "source"
entry_point = "phdb_plugin_example.plugin:WeatherLogPlugin"

[source]
emits = ["Observation"]
entity_refs = []
formats_used = []
records_required = []
sidecars = []
embeddable_tables = []
facets_projected = ["Time"]
